apply plugin: 'android'


dependencies {
    compile project(':HighlightCalendarView')
}

android {
    compileSdkVersion 19
    buildToolsVersion "20"

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 19;
        versionCode 1
        versionName "1.0.0-SNAPSHOT"
    }
}
